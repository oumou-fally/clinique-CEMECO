# ============ DATABASE ============
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=clinique_cemeco
DB_PORT=3306

# ============ CONNECTION POOL ============
DB_POOL_MAX=10
DB_POOL_MIN=2
DB_POOL_ACQUIRE_TIMEOUT=30000
DB_POOL_IDLE_TIMEOUT=30000

# ============ SERVER ============
PORT=3000
NODE_ENV=development

# ============ LOGS ============
LOG_LEVEL=info

# Le système d'email utilise Gmail SMTP en production via backend/config/mailer.js

# ============ EMAIL ============
# Utilisez un mot de passe d'application Gmail (App Password) pour SMTP sécurisé.
# Ne mettez jamais votre mot de passe Gmail principal si l'authentification à deux facteurs est activée.
# Assurez-vous que l'adresse EMAIL_USER est une adresse Gmail active.
EMAIL_USER=cliniquecemeco14@gmail.com
EMAIL_PASS=pjky aseb etlb tfks
# EMAIL_FROM peut être personnalisé si nécessaire ; sinon, l'adresse Gmail est utilisée.
# EMAIL_FROM=cliniquecemeco14@gmail.com
